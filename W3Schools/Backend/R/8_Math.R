
# R: W3Schools - Main Tutorial
# Section 8: Math

# Operators
    #   +	Addition
    #   -	Subtraction
    #   *	Multiplication
    #   /	Division
    #   ^	Exponentiation

# Math Functions
    # abs(x)	            Absolute value
    # sqrt(x)	            Square root
    # ceiling(x)	        Rounds up to the nearest integer
    # floor(x)	            Rounds down to the nearest integer
    # trunc(x)	            Truncates to the nearest integer
    # round(x, digits=n)	Rounds to n digits
    # signif(x, digits=n)	Rounds to n significant digits
    